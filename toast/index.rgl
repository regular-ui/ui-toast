<div class="m-toast m-toast-{position} {class}" r-hide={!visible}>
    {#list list as item}
    <div class="toast_item toast_item-{item.state}" r-animation={animation}>
        {item.text}
        <a class="toast_close" on-click={this.close(item)}></a>
    </div>
    {/list}
</div>
